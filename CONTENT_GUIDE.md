# 内容管理指南 | Content Management Guide

本文档介绍如何管理和更新网站内容。

## 📁 目录结构

```
content/
├── siteMetadata.yml    # 站点配置
├── about.yml           # 关于页面
├── portfolio.yml       # 作品集
├── blog/               # 已发布文章（按分类）
│   ├── AI/
│   ├── Systems/
│   ├── Web/
│   └── Meta/
├── drafts/             # 草稿
├── projects/           # 项目详情页
├── config/             # 内容配置
│   └── tags.ts         # 标签定义
└── locales/            # 国际化文本
```

---

## 🛠️ CLI 命令速查

| 命令                    | 说明               |
| ----------------------- | ------------------ |
| `npm run new`           | 交互式创建新文章   |
| `npm run publish`       | 发布草稿到正式目录 |
| `npm run unpublish`     | 将文章移回草稿     |
| `npm run stats`         | 查看内容统计       |
| `npm run list-tags`     | 列出所有可用标签   |
| `npm run validate-tags` | 验证文章标签       |

---

## ✍️ 创建新文章

**方法一：使用 CLI（推荐）**

```bash
npm run new
# ? 文章标题: RAG 系统最佳实践
# ? 分类: AI
# ? 标签: LLM, deep-learning
# ✅ 已创建: content/blog/AI/rag-best-practices.md
```

**方法二：手动创建**

在 `content/blog/{分类}/` 下创建 `.md` 文件：

```markdown
---
title: "文章标题"
date: "2024-01-15"
tags: [LLM, deep-learning]
draft: false
summary: "文章摘要"
authors: ["default"]
---

正文内容...
```

---

## 📝 草稿管理

**保存为草稿**

- 使用 `npm run new` 时选择「保存为草稿」
- 或手动放到 `content/drafts/` 目录

**发布草稿**

```bash
npm run publish
# 选择草稿 → 选择分类 → 完成发布
```

**取消发布**

```bash
npm run unpublish
# 选择文章 → 移回 drafts/
```

---

## 📊 查看统计

```bash
npm run stats

# 📚 已发布文章: 21 篇
# ──────────────────────
#   Systems   10 ██████████
#   AI         9 █████████
# 📝 草稿: 3 篇
```

---

## 🔢 LaTeX 公式

```markdown
行内：$E = mc^2$
块级：$$\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}$$
```

---

## 🚀 本地预览

```bash
npm run dev
```

访问 http://localhost:3000 预览更改。
