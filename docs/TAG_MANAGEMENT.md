# 标签管理系统

本项目使用集中式标签管理系统，确保博客标签的一致性和规范性。

## 核心文件

- **`lib/tags.ts`**: 标签定义和验证逻辑
  - 定义所有允许的标签（按类别分组）
  - 标签别名系统（支持中文、缩写等）
  - 验证和标准化函数

- **`scripts/list-tags.ts`**: 查看所有可用标签
- **`scripts/validate-tags.ts`**: 验证和修复标签

## 使用方法

### 1. 查看可用标签

```bash
npm run list-tags
```

这会显示所有可用标签，按类别分组，并显示每个标签的别名（如果有）。

### 2. 验证标签

在提交前验证所有博客文章的标签：

```bash
npm run validate-tags
```

这会检查所有标签是否符合规范，并显示统计信息。

### 3. 自动修复标签

如果发现不规范的标签（如使用了中文或缩写），可以自动修复：

```bash
npm run validate-tags --fix
```

这会自动将别名转换为标准标签。例如：

- `深度学习` → `deep-learning`
- `DL` → `deep-learning`
- `Vector DB` → `vector-db`

## 写博客时的建议

### ✅ 推荐做法

1. **自由使用别名**：写作时可以使用中文或缩写，例如：

   ```yaml
   tags: [深度学习, LLM, 性能优化]
   ```

2. **运行验证**：写完后运行 `npm run validate-tags --fix` 自动规范化

3. **查看可用标签**：不确定用什么标签时，运行 `npm run list-tags` 查看所有选项

### 标签类别

当前标签分为以下类别：

- **AI**: AI、机器学习、深度学习相关
- **SYSTEMS**: 系统编程、性能优化
- **DEV**: 开发工具
- **FRAMEWORKS**: 框架和库
- **TECHNIQUES**: 技术和方法
- **WEB**: Web 开发
- **META**: 博客元信息
- **TECH**: 特定技术

## 添加新标签

如果需要添加新标签：

1. 编辑 `lib/tags.ts`
2. 在对应的类别中添加标签
3. 如果需要，添加别名到 `TAG_ALIASES`
4. 运行 `npm run validate-tags` 确认更改

## 标签规范

- 使用小写字母和连字符（如 `deep-learning`）
- 专有名词使用 PascalCase（如 `ChatGLM`, `AutoGen`）
- 缩写全大写（如 `AI`, `LLM`, `HPC`）
- 避免空格，使用连字符代替（`vector-db` 不是 `vector DB`）

## 示例工作流

```bash
# 1. 查看可用标签
npm run list-tags

# 2. 写博客（使用中文标签也可以）
echo '---
title: "我的新文章"
tags: [深度学习, ChatGLM]
---' > content/blog/AI/new-post.md

# 3. 验证和修复标签
npm run validate-tags --fix

# 4. 重新构建
npx contentlayer2 build

# 5. 提交
git add . && git commit -m "feat: add new post"
```

## 好处

✨ **一致性**：所有标签遵循统一标准
🚀 **便捷性**：写作时可以用中文或缩写
🔍 **可发现性**：轻松查看所有可用标签
🛡️ **验证**：防止拼写错误和不规范标签
📊 **统计**：了解标签使用情况
